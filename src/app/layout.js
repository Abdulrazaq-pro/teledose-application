import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "TeledoseAI",
  description:
    "TeleDose is an innovative telemedicine app designed to provide convenient, AI-driven healthcare support from the comfort of home. With features like symptom checking, personalized health insights, and a responsive chatbot, TeleDose empowers users to get reliable medical guidance, access health resources, and manage their wellness anytime, anywhere.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
